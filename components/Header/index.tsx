import { NeptuneLogo, NeptuneLogoText } from "@svg";
import { classNames } from "@utils/functions";
import Head from "next/head";
import Link from "next/link";
import { FC } from "react";

interface HeaderProps {
  title?: string;
  navKey?: string;
}

const Header: FC<HeaderProps> = ({
  title = "NeptuneMutual - Cover Fee Calculator",
  navKey = "",
}) => {
  type NavlinkType = { label: string; href: string };
  const navLinks: NavlinkType[] = [
    {
      label: "Home",
      href: "/",
    },
    {
      label: "Whitelist",
      href: "/whitelist",
    },
    {
      label: "Payouts",
      href: "/payouts",
    },
    {
      label: "Calculator",
      href: "/calculator",
    },
  ];

  return (
    <div>
      <Head>
        <title>{title}</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className="flex flex-wrap items-center w-full px-4 text-white gap-x-15 sm:px-8 bg-prim-blue">
        <Link href="/">
          <a className="block py-3 w-min">
            <div className="flex items-center gap-2">
              <NeptuneLogo />
              <NeptuneLogoText height={35} />
            </div>
          </a>
        </Link>
        <div className="flex text-sm gap-7 font-poppins">
          {navLinks.map(({ href, label }, idx) => (
            <Link href={href} key={idx}>
              <a
                className={classNames(
                  "block py-8",
                  href.substring(1) === navKey
                    ? "box-border font-semibold border-b-4 rounded-sm border-prim-border text-text-prim"
                    : "text-text-gray font-normal"
                )}
              >
                {label}
              </a>
            </Link>
          ))}
        </div>
      </div>
    </div>
  );
};

export default Header;
